<Window x:Class="FitnessCenterIS.View.Windows.QRCodeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FitnessCenterIS.View.Windows"
        xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
        mc:Ignorable="d"
        Title="QRCodeWin"
        Height="500"
        Width="400"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Border Background="#f0f0f0" BorderBrush="#090909" BorderThickness="1" CornerRadius="15">


            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="20" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>





                <TextBlock Grid.Row="1" Text="Scan QR code" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0 5"
                           FontSize="25" FontWeight="DemiBold"/>
                <TextBlock Grid.Row="1" Text="Отсканируйте QR-код, чтобы найти клиента в системе"
                           HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0 45 0 0"
                           FontSize="12" FontWeight="DemiBold"/>

                <Border Grid.Row="1" Margin="100 80" Background="#090909">

                    <Grid>
                        <Image x:Name="CameraPreview" Stretch="UniformToFill" />

                        <Grid>
                            <Rectangle Fill="#fff" Width="3" HorizontalAlignment="Left" Margin="0,150,0,0"/>
                            <Rectangle Fill="#fff" Width="3" HorizontalAlignment="Left" Margin="0,0,0,150"/>
                            <Rectangle Fill="#fff" Width="3" HorizontalAlignment="Right" Margin="0,150,0,0"/>
                            <Rectangle Fill="#fff" Width="3" HorizontalAlignment="Right" Margin="0,0,0,150"/>


                            <Rectangle Fill="#fff" Height="3" VerticalAlignment="Top" Margin="150,0,0,0"/>
                            <Rectangle Fill="#fff" Height="3" VerticalAlignment="Top" Margin="0,0,150,0"/>
                            <Rectangle Fill="#fff" Height="3" VerticalAlignment="Bottom" Margin="0,0,150,0"/>
                            <Rectangle Fill="#fff" Height="3" VerticalAlignment="Bottom" Margin="150,0,0,0"/>
                        </Grid>
                    </Grid>
                </Border>
                <StackPanel Grid.Row="2" HorizontalAlignment="Center" Margin="10">
                    <Button x:Name="ScanButton" Background="#000" BorderBrush="Transparent" Margin="5" Width="350" Click="ScanButton_Click">
                        <WrapPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="Сканировать" Margin="0 0 10 0"/>
                            <Icon:MaterialDesign Kind="QrCodeScannerSharp" />
                        </WrapPanel>
                    </Button>
                    <Button Background="#000" BorderBrush="Transparent" Margin="5" Click="StopScanButton_Click">
                        <WrapPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="Назад" Margin="0 0 10 0"/>
                        </WrapPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>