<Window x:Class="FitnessCenterIS.View.Windows.ReceiptPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
        mc:Ignorable="d"
        Title="Предварительный просмотр чека" 
        Height="700" Width="600"
        Background="Transparent"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True">

    <Border Background="#f0f0f0" CornerRadius="15" BorderBrush="#090909" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <!-- Заголовок -->
                <RowDefinition Height="*"/>
                <!-- Основной контент -->
                <RowDefinition Height="60"/>
                <!-- Кнопки внизу -->
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <Border Grid.Row="0" Background="#090909" CornerRadius="15,15,0,0">
                <Grid>
                    <TextBlock Text="Предварительный просмотр чека" 
                               HorizontalAlignment="Left" VerticalAlignment="Center"
                               FontSize="22" FontWeight="SemiBold" Foreground="White" Margin="20,0,0,0"/>
                    <Button Click="CloseButton_Click" Width="30" Height="30" 
                            HorizontalAlignment="Right" VerticalAlignment="Center" 
                            Background="Transparent" BorderThickness="0" Margin="0,0,20,0">
                        <Icon:PackIconMaterial Kind="Close" Width="20" Height="20" Foreground="White"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Просмотр документа -->
            <ScrollViewer Grid.Row="1" Margin="10">
                <RichTextBox x:Name="ReceiptTextBox" IsReadOnly="True" 
                           BorderThickness="0" Background="White">
                    <RichTextBox.Resources>
                        <Style TargetType="{x:Type Paragraph}">
                            <Setter Property="Margin" Value="0"/>
                        </Style>
                    </RichTextBox.Resources>
                </RichTextBox>
            </ScrollViewer>

            <!-- Кнопки внизу -->
            <Border Grid.Row="2" Background="#f0f0f0" CornerRadius="0,0,15,15">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,10">
                    <Button x:Name="PrintButton" Click="PrintButton_Click" 
                        Background="#090909" BorderBrush="Transparent" Foreground="White"
                        Height="40" Margin="0,0,10,0" Padding="20,0">
                        <StackPanel Orientation="Horizontal">
                            <Icon:PackIconMaterial Kind="Printer" Margin="0,0,8,0"/>
                            <TextBlock Text="Печать"/>
                        </StackPanel>
                    </Button>
                    <Button Click="CloseButton_Click" Background="#f5f5f5" BorderThickness="1" 
                            BorderBrush="#dedede" Height="40" Padding="20,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Закрыть"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>