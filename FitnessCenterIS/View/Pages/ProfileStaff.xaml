<Page x:Class="FitnessCenterIS.View.Pages.ProfileStaff"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FitnessCenterIS.View.Pages"
      mc:Ignorable="d" 
      xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
      Title="Профиль сотрудника"
      Loaded="StaffProfilePage_Loaded">

    <Page.Resources>
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#2c3e50"/>
            <Setter Property="Margin" Value="0,25,0,15"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Background" Value="#ecf0f1"/>
        </Style>

        <Style x:Key="DataGridStyle" TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#bdc3c7"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#f9f9f9"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#ecf0f1"/>
            <Setter Property="Margin" Value="0,5,0,15"/>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#34495e"/>
            <Setter Property="Margin" Value="0,5,10,5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ValueStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#2c3e50"/>
            <Setter Property="Margin" Value="0,5,0,5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="StatusChipStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="Padding" Value="10,3"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
    </Page.Resources>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <Grid Margin="20" Background="White">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Шапка профиля -->
                <Border Grid.Row="0" Background="#090909" CornerRadius="10,10,0,0" Padding="20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Фото сотрудника -->
                        <Border Grid.Column="0" Width="150" Height="150" CornerRadius="75" 
                            BorderBrush="White" BorderThickness="3" Margin="0,0,20,0"
                            ClipToBounds="True">
                            <Border.Background>
                                <ImageBrush x:Name="StaffImage" Stretch="UniformToFill"/>
                            </Border.Background>
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="2" BlurRadius="5" Color="#22000000"/>
                            </Border.Effect>
                        </Border>

                        <!-- Основная информация -->
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock x:Name="StaffFullName" FontSize="28" FontWeight="Bold" Foreground="White"/>
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,10">
                                <Border x:Name="RoleBorder" Style="{StaticResource StatusChipStyle}" Background="#3498db">
                                    <StackPanel Orientation="Horizontal">
                                        <Icon:PackIconMaterial Kind="AccountTie" Width="14" Height="14" Margin="0,0,5,0" Foreground="White" VerticalAlignment="Center"/>
                                        <TextBlock x:Name="StaffRole" FontSize="14" Foreground="White"/>
                                    </StackPanel>
                                </Border>
                                <Border Margin="10,0,0,0" Style="{StaticResource StatusChipStyle}" Background="#27ae60">
                                    <StackPanel Orientation="Horizontal">
                                        <Icon:PackIconMaterial Kind="Calendar" Width="14" Height="14" Margin="0,0,5,0" Foreground="White" VerticalAlignment="Center"/>
                                        <TextBlock x:Name="StaffHireDate" FontSize="14" Foreground="White"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <Icon:PackIconMaterial Kind="Phone" Width="16" Height="16" Margin="0,0,5,0" Foreground="White" VerticalAlignment="Center"/>
                                <TextBlock x:Name="StaffPhoneNumber" Foreground="White" VerticalAlignment="Center"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <Icon:PackIconMaterial Kind="Email" Width="16" Height="16" Margin="0,0,5,0" Foreground="White" VerticalAlignment="Center"/>
                                <TextBlock x:Name="StaffEmail" Foreground="White" VerticalAlignment="Center"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- QR-код -->
                        <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                            <Border Background="White" CornerRadius="5" Padding="5" Width="100" Height="100">
                                <Image x:Name="StaffQRCodeImage" Stretch="Uniform"/>
                            </Border>
                            <TextBlock Text="QR-код сотрудника" Foreground="White" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Основное содержимое профиля -->
                <Border Grid.Row="1" Background="White" CornerRadius="0,0,10,10" 
                    BorderThickness="1" BorderBrush="#e0e0e0" Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <!-- Личная информация -->
                            <RowDefinition Height="Auto"/>
                            <!-- Рабочая информация -->
                            <RowDefinition Height="Auto"/>
                            <!-- Закрепленные услуги -->
                            <RowDefinition Height="Auto"/>
                            <!-- История посещений -->
                            <RowDefinition Height="Auto"/>
                            <!-- Заметки -->
                        </Grid.RowDefinitions>

                        <!-- Личная информация -->
                        <StackPanel Grid.Row="0">
                            <TextBlock Text="Личная информация" Style="{StaticResource SectionHeaderStyle}"/>
                            <Grid Margin="10,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Левая колонка -->
                                <StackPanel Grid.Column="0" Grid.Row="0">
                                    <TextBlock Text="ID сотрудника:" Style="{StaticResource LabelStyle}"/>
                                    <Border Background="#f8f9fa" CornerRadius="4" Padding="10,8">
                                        <TextBlock x:Name="StaffIDTextBlock" Style="{StaticResource ValueStyle}"/>
                                    </Border>
                                </StackPanel>

                                <StackPanel Grid.Column="0" Grid.Row="1" Margin="0,10,10,0">
                                    <TextBlock Text="Дата рождения:" Style="{StaticResource LabelStyle}"/>
                                    <Border Background="#f8f9fa" CornerRadius="4" Padding="10,8">
                                        <TextBlock x:Name="StaffDateOfBirth" Style="{StaticResource ValueStyle}"/>
                                    </Border>
                                </StackPanel>

                                <StackPanel Grid.Column="0" Grid.Row="2" Margin="0,10,10,0">
                                    <TextBlock Text="Пол:" Style="{StaticResource LabelStyle}"/>
                                    <Border Background="#f8f9fa" CornerRadius="4" Padding="10,8">
                                        <TextBlock x:Name="StaffGender" Style="{StaticResource ValueStyle}"/>
                                    </Border>
                                </StackPanel>

                                <!-- Правая колонка -->
                                <StackPanel Grid.Column="1" Grid.Row="0" Margin="10,0,0,0">
                                    <TextBlock Text="Номер карты:" Style="{StaticResource LabelStyle}"/>
                                    <Border Background="#f8f9fa" CornerRadius="4" Padding="10,8">
                                        <TextBlock x:Name="StaffCardNumber" Style="{StaticResource ValueStyle}"/>
                                    </Border>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Grid.Row="1" Margin="10,10,0,0">
                                    <TextBlock Text="Адрес:" Style="{StaticResource LabelStyle}"/>
                                    <Border Background="#f8f9fa" CornerRadius="4" Padding="10,8">
                                        <TextBlock x:Name="StaffAddress" Style="{StaticResource ValueStyle}" TextWrapping="Wrap"/>
                                    </Border>
                                </StackPanel>
                            </Grid>
                        </StackPanel>

                        <!-- Рабочая информация -->
                        <StackPanel Grid.Row="1">
                            <TextBlock Text="Рабочая информация" Style="{StaticResource SectionHeaderStyle}"/>
                            <Grid Margin="10,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Роль -->
                                <Border Grid.Column="0" Background="#e8f4fd" CornerRadius="8" Padding="15" Margin="0,0,10,0">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal">
                                            <Icon:PackIconMaterial Kind="AccountTie" Width="24" Height="24" Foreground="#2980b9" VerticalAlignment="Center"/>
                                            <TextBlock Text="Должность" FontWeight="SemiBold" FontSize="16" Margin="10,0,0,0" Foreground="#2980b9"/>
                                        </StackPanel>
                                        <TextBlock x:Name="StaffRoleDetail" FontSize="18" FontWeight="Bold" Margin="0,10,0,0" Foreground="#2980b9"/>
                                        <TextBlock x:Name="StaffRoleDescription" Foreground="#3498db" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Border>

                                <!-- Дата найма -->
                                <Border Grid.Column="1" Background="#e8f7f2" CornerRadius="8" Padding="15" Margin="10,0,0,0">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal">
                                            <Icon:PackIconMaterial Kind="Calendar" Width="24" Height="24" Foreground="#27ae60" VerticalAlignment="Center"/>
                                            <TextBlock Text="Дата найма" FontWeight="SemiBold" FontSize="16" Margin="10,0,0,0" Foreground="#27ae60"/>
                                        </StackPanel>
                                        <TextBlock x:Name="StaffHireDateDetail" FontSize="18" FontWeight="Bold" Margin="0,10,0,0" Foreground="#27ae60"/>
                                        <TextBlock x:Name="StaffWorkExperience" Foreground="#2ecc71"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </StackPanel>

                        <!-- Закрепленные услуги -->
                        <StackPanel Grid.Row="2">
                            <TextBlock Text="Закрепленные услуги" Style="{StaticResource SectionHeaderStyle}"/>
                            <DataGrid x:Name="StaffServicesGrid" Style="{StaticResource DataGridStyle}" Height="Auto" MaxHeight="250">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Название услуги" Binding="{Binding ServiceName}" Width="*"/>
                                    <DataGridTextColumn Header="Тип услуги" Binding="{Binding ServiceType}" Width="Auto"/>
                                    <DataGridTextColumn Header="Классификация" Binding="{Binding ServiceClassification}" Width="Auto"/>
                                    <DataGridTextColumn Header="Цена" Binding="{Binding Price, StringFormat={}{0:N0} ₽}" Width="Auto"/>
                                    <DataGridTemplateColumn Header="Статус" Width="Auto">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Border CornerRadius="12" Padding="8,3" Margin="5,2" HorizontalAlignment="Center">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Background" Value="#2ecc71"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding StatusService}" Value="Неактивна">
                                                                    <Setter Property="Background" Value="#e74c3c"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding StatusService}" Value="Приостановлена">
                                                                    <Setter Property="Background" Value="#f39c12"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <TextBlock Text="{Binding StatusService}" Foreground="White" FontWeight="SemiBold"/>
                                                </Border>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>

                        <!-- История посещений -->
                        <StackPanel Grid.Row="3">
                            <TextBlock Text="История посещений" Style="{StaticResource SectionHeaderStyle}"/>
                            <DataGrid x:Name="StaffAttendancesGrid" Style="{StaticResource DataGridStyle}" Height="Auto" MaxHeight="200">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Дата" Binding="{Binding EntryDateTime, StringFormat=dd.MM.yyyy}" Width="Auto"/>
                                    <DataGridTextColumn Header="Время входа" Binding="{Binding EntryDateTime, StringFormat=HH:mm}" Width="Auto"/>
                                    <DataGridTextColumn Header="Время выхода" Binding="{Binding ExitDateTime, StringFormat=HH:mm}" Width="Auto"/>
                                    <DataGridTextColumn Header="Продолжительность" Binding="{Binding FormattedDuration}" Width="Auto"/>
                                    <DataGridTextColumn Header="Шкафчик" Binding="{Binding KeyNumber}" Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>

                        <!-- Заметки -->
                        <StackPanel Grid.Row="4">
                            <TextBlock Text="Заметки" Style="{StaticResource SectionHeaderStyle}"/>
                            <Border Background="#f8f9fa" CornerRadius="5" Padding="15" Margin="10,0">
                                <TextBlock x:Name="StaffNotesTextBox" TextWrapping="Wrap" Foreground="#34495e"/>
                            </Border>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
